{
  "name": "idle-workers-blog",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "start": "gatsby develop",
    "build": "rm -rf public && gatsby build",
    "clean": "gatsby clean",
    "deploy": "npm run build && cp now.json public/ && cd public && now alias $(now) blog.idleworkers.com"
  },
  "dependencies": {
    "gatsby": "^4.25.7",
    "gatsby-plugin-manifest": "^2.2.39",
    "gatsby-plugin-offline": "^3.0.32",
    "gatsby-plugin-react-helmet": "^3.1.21",
    "gatsby-theme-blog": "^1.2.32",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-helmet": "^5.2.1"
  }
}
