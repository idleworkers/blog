{
  "name": "idle-workers-blog",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "start": "gatsby develop",
    "build": "rm -rf public && gatsby build",
    "clean": "gatsby clean",
    "deploy": "gatsby build && cp now.json public/ && cd public && now alias $(now) blog.idleworkers.com"
  },
  "dependencies": {
    "gatsby": "^2.19.5",
    "gatsby-theme-blog": "^1.2.32",
    "react": "^16.12.0",
    "react-dom": "^16.12.0"
  }
}
